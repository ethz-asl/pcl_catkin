set(@PROJECT_NAME@_INCLUDE_DIRS @CATKIN_DEVEL_PREFIX@/include/pcl/)

# The following pcl_catkin_enforce macro can be called in a depending
# project's CMakeLists.txt file (Below findpackage pcl_catkin or the
# catkin_simple() call.
# It will ensure that pcl_catkin's lib PCL will be included instead
# of any other possible candidate (like a system installation of PCL).
#
# It is possible to get that right by careful treatment of workspace layer
# order but calling this macro is easier.
macro(pcl_catkin_enforce)
	include_directories(BEFORE ${@PROJECT_NAME@_INCLUDE_DIRS})
endmacro()
